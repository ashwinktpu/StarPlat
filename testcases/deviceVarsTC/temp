propNode<int> prop;

do{
    forall(v in g.nodes())
    {
        v.prop = 10;
    }
}while(True);

copyProptogpu;
do{
    forall(v in g.nodes())
    {
        v.prop = 10;
    }
}while(True);
copyPropfromGPU;