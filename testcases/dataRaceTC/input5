function test(Graph g)
{
    int x;
    
    propNode<int> prop;
    g.attachNodeProperty(prop = 10);
    forall(v in g.nodes())
    {
        int l1;
        int l2;
        
        x = x + (l1++);
        x = x + (l2++);
    }
}