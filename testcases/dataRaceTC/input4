function test(Graph g)
{
    int global = 0;
    int x;
    int y;
    
    propNode<int> prop;
    g.attachNodeProperty(prop = 10);
    forall(v in g.nodes())
    {
        int l1;
        int l2;
        
        global = 10;
        x = x + (l1++);
        y = (v.prop++) + y;
    }
}