function compute_val(Graph g,int c)
{
    propNode<bool> modified;
    propEdge<int> weight;

    g.attachNodeProperty(modified=false);
    g.attachEdgeProperty(weight=0);
}

function hello_func(Graph g1, float d)
{
    int value=2;
    float a=0;
    a=value+d;
}