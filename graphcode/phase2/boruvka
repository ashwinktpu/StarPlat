function MST(Graph g) {
  propNode<int> cheapest;
  g.attachNodeProperty(cheapest = 0);

  bool finished = False;
  
  fixedPoint until (finished: !cheapest) {
    forall (v in v.nodes()) {
      v.cheapest = 3;
    }
  }
}
