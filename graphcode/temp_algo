function count_nodes(Graph g)
{
    int x;
    int y;
    int z;
    int w = 0;

    propNode<int> prop;
    y = x+10;
    z = x;

    g.attachNodeProperty(prop = (x || z));

    forall(v in g.nodes()) {
        v.prop = y;
    }
}